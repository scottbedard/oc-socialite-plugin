# ===================================
#  Form Field Definitions
# ===================================

fields:
    is_enabled:
        label: bedard.socialite::lang.api.fields.is_enabled
        comment: bedard.socialite::lang.api.fields.is_enabled_comment
        type: switch
        span: left
